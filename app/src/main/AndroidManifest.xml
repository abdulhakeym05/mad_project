<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abdulmadproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AbdulMadProject">
        <activity android:name=".ViewFeedbacks"></activity>
        <activity android:name=".FeedbackUpload" />
        <activity android:name=".ForgetPassowrd" />
        <activity android:name=".StudentRegister" />
        <activity android:name=".Admin" />
        <activity android:name=".StudentLogin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>